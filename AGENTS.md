# Agent notes — Mistral Impianti

Next.js 16: APIs/conventions may differ from training data. Prefer `node_modules/next/dist/docs/` when unsure.

This app merges ticketing (pratiche/operatori) + rapportini into one Prisma schema and NextAuth session.
